<p id="notice"><%= notice %></p>
<div class="container">
  
  <h2>
    <%= @post.title %>
  </h2><hr>

  <p>
    <%= @post.content %>
  </p>
  <p>
    <strong>Date:</strong>
    <%= @post.created_at.to_date %>
  </p>
</div>
<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
